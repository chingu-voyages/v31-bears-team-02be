import knex from 'knex';
import config from '../config';


// Game Data Access Object (used to interface w/ db)
class GameDAO {
    async createGame(db, id) {

		// TO DO??
	}
}


export default new GameDAO();